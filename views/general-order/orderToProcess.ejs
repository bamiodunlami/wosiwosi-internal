
<%- include('include/header') -%>
    <section class="order-page">
        <div class="container">
            <h6 class="alert alert-success text-center">Order to process</h6>
            <!-- <div class="row d-flex justify-content-center alert alert-light">
                <div class="col-md-6 dateFilter">
                        <form action="/order" method="get" class="row">
                            <div class="dateFilter-item col-sm-12">
                                <input type="text" placeholder="Search Order..." class="form-control" name="fromDate" value="">
                            </div>
                        </form>
                </div>
            </div> -->
            <!-- table -->
            <div class="row order-table">
                <div class="col-12">
                    <table class="table table-striped table-hover ">
                        <thead>
                        <tr>
                            <th scope="col">Order</th>
                            <th scope="col">-</th>
                            <th scope="col">-</th>
                            <th scope="col">-</th>
                        </tr>
                        </thead>
                        <tbody class="">
                            <% for(i =0; i<order.length; i++ ){%>
                                <tr>
                                    <td><a href="/single-order-processing?id=<%=order[i]%>"><%=order[i]%></a></td>
                                    <!-- ?id=<=order[i].id>&fromDate=<=fromDate>&toDate=<=toDate>&page=<=page>&fromTime=<=fromTime>&toTime=<=toTime> -->
                                    <td>picked</td>
                                    <td>Packed</td>
                                    <td>no coment</td>
                                </tr>
                            <%}%>
                        </tbody>
                    </table>
                </div>
            </div>
        
            <!-- back to dashboard -->
            <div class="row">
                <div class="text-center">
                    <a class="btn btn-success btn-md" href="/admin/online">Back to dashboard</a>
                </div>
            </div>

        </div>
    </section>

    <%- include('include/footerTemp') -%>
    <%- include('include/footer') -%>
